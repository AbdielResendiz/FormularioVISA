import { PDFViewer, Page, Image as ImagePDF,Text as TextPDF, View as ViewPDF, Document, StyleSheet } from '@react-pdf/renderer';

const datosParteja=(ec)=>{
    switch (ec) {
        case 0:
            return(
                <>
                </>
            );

        case 1:
            return(
                <>
                </>
            );
        case 2:
            return(
                <>
                </>
            );   

        case 3:
            return(
                <>
                </>
            );  

        case 4:
            return(
                <>
                </>
            );  
    
    
        default:
            break;
    }
}
export default datosParteja;